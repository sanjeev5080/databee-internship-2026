apiVersion: v1
kind: Service
metadata:
  name: gothenburg-weather-service
spec:
  type: NodePort
  selector:
    app: gothenburg-weather
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
